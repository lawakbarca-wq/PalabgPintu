
  # Import HTML as Editable Design (Copy) (Copy) (Copy)

  This is a code bundle for Import HTML as Editable Design (Copy) (Copy) (Copy). The original project is available at https://www.figma.com/design/gj4iE0WWtjRhPqnumcnmnq/Import-HTML-as-Editable-Design--Copy---Copy---Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  